Run all executable business-idea formulation strategies from C:\dev\Inc using agent capabilities (do not use the interactive run_all_strategies.py menu unless explicitly requested): include strategies 1, 5, 6, 7, 9, 11, 12, 13, 14, and 15; skip 3, 4 (verbal/network/questionnaire) and retired 8 and 10; Strategy 2 remains verbal-only (skip); automate what you can (fetch RSS/API/OurWorldInData data, run strategy scripts in non-interactive or subprocess mode where supported—e.g. Strategy 1 with --non-interactive or --inputs when useful for scaffolding only, Strategy 15 with --non-interactive and --inputs, Strategy 12 with --non-interactive and --inputs when useful for Mode B scoring aids—and perform Prompt 1a/1b/1c synthesis yourself when scripts block on input() or clipboard prompts); if a strategy cannot complete without code changes, report the blocker and continue with the rest; read-only on the codebase (no edits to strategy scripts, configs, task.md, or other existing files)—write-only for new artifacts you create this session; de-duplicate against past_business_ideas.md and recent business_ideas_*.md (agent-business-idea-runs/outputs/ and strategy folders); deliver ranked, Nigeria-focused ideas with strategy trace; for Strategy 12 run BOTH Mode A and Mode B (see Strategy 12 dual-mode below)—never Mode B overlay alone; Mode B GUEMF scores use 1-5 per Growing/Urgent/Expensive/Mandatory/Frequent (composite max 25) on ideas from other strategies; and execution notes in one new output file only under agent-business-idea-runs/outputs/ (e.g. business_ideas_YYYYMMDD.md); in that file, always assess the generated ideas (commercial viability, MVP cost ≤~$30k where applicable, regulatory fit, dedup confidence, and founder/operator fit when inferable); for every idea in the ranked set, include a short Competitors / alternatives note (2–4 items): named products, agencies, or “DIY/manual/spreadsheet”; for each, one line on why a Nigeria buyer might still choose this idea (e.g. Naira/Paystack, niche focus, language, price, distribution)—not a full market map; Best ideas (top 3–5) must each have this competitors note in their idea-detail section; prefer real names when known; if unknown, say so and list the obvious substitute pattern (DIY, accountant, WhatsApp, foreign SaaS); do not invent fake competitor companies or fake pricing; and include a clear "Best ideas" section at the top naming the top 3–5 recommendations with one-line rationale each, plus a ranked table for the full set; note data freshness, partial runs, and per-strategy execution status in an execution summary (each strategy status must be one of: ran / synthesized / skipped / blocked—Strategy 1 empty or failed online discovery must not abort the whole run); end the file with suggested next validation steps and dates; use today's local date in filenames; on Windows use PYTHONIOENCODING=utf-8 for subprocess strategy runs; if a script blocks on input() or clipboard helpers, timeout, report, and synthesize; for Strategy 1 use formula Successful Business + Recurring Complaint = Profitable Variation—every S1-traced idea must name a successful business and a recurring complaint then a differentiated variation (not Strategy 6 niche-combination and not Strategy 7 trending-adaptation alone); Strategy 1 must always discover successful businesses/startups online and always discover recurring gaps/complaints online (web search, news, reviews, forums, app-store text, credible directories)—do not use Strategy-1-Business-Variation/seed_businesses.json (archived), canned example_complaints, or AI-invented gaps as the problem statement or as the sole evidence that a business is successful or that a complaint is real; prefer fetch JSON key strategy_1_discovery (agent-native web research + optional discovery_leads from RSS/Product Hunt/StartupList — leads are optional starting points only, not proven complaints); every S1-traced idea in the markdown/Docx must include citeable online provenance for the complaint (source name, title or short quote, URL, and date when available) and preferably a citeable online signal that the named business is successful; if online discovery fails for S1, mark status blocked or synthesized with an explicit note that citations are missing and continue—never fall back to seed_businesses.json; for Strategy 12 (GUEMF / High-Value Problem Filtering) ALWAYS run dual-mode: Mode A (standalone)—Prompt-1a-style: find problems that are Growing, Urgent, Expensive to solve, Mandatory, and Frequent, then produce Nigeria-focused business ideas whose primary strategy trace includes S12 (same ranked table as other ideas; include short GUEMF rationale per Mode A idea); Mode B (overlay)—apply GUEMF 1-5 scores + composite to ideas originating from other strategies in the ranked table; execution summary for Strategy 12 must explicitly note BOTH Mode A and Mode B (e.g. Mode A synthesized/ran + Mode B applied)—status may be synthesized or ran; optional aid: Business-Idea-Formulation-Strategy-12-High-Value-Problem-Filtering/problem_filter.py --non-interactive --inputs fixtures/sample_inputs.json (or a run-local JSON with complete criteria_scores)—do not invent Y/N scores; if the interactive script blocks, timeout and synthesize Mode A/B yourself; incomplete Strategy 12 = Mode B columns only with no S12-traced Mode A ideas; for Strategy 15 prefer import-only validation or prior audited telecom figures over overwriting strategy-folder outputs; reuse agent-business-idea-runs/agent_strategy_run.py for RSS/OWID plus Strategy 1 discovery plus Strategy 6/7 inputs (from repo root: python agent-business-idea-runs/agent_strategy_run.py; writes fetch JSON to agent-business-idea-runs/inputs/ including strategy_1_discovery, strategy_6_startup_directory, and strategy_7_trending when available; strategy_1_seeds is removed—do not expect it; Strategy 15 skipped by default—use --with-strategy15 only if needed); for Strategy 6 prefer StartupList Africa Nigeria startup directory data from fetch JSON or agent synthesis—Crunchbase is optional legacy fallback only, not required; for Strategy 7 prefer Product Hunt RSS / Techpoint headlines from fetch JSON or agent synthesis—Crunchbase Trending Profiles screenshot is optional legacy only; if S1 online discovery or S6/S7 fetches fail, log in execution summary and continue (for S1 do not fall back to seed_businesses.json; synthesize S6/S7 from RSS/OWID as today); target 10–14 deduped ideas; finish the markdown completely first (including Best ideas, ranked table, idea details, commercial viability, execution summary, and next-validation steps—leave a placeholder only for the Docx conversion status line if needed); then convert/open the Word file exactly once as the final automated step—never call convert/open mid-draft, after small .md tweaks, or a second time to refresh an already-opened document (a second regenerate_and_open_docx closes Word and defeats auto-open); one output file only under agent-business-idea-runs/outputs/ (business_ideas_YYYYMMDD.md) plus its paired Word doc beside it (business_ideas_YYYYMMDD.docx; treat ".doc" as Word output)—prefer business_bookmark_sorter/docx_export.py regenerate_and_open_docx if importable with sys.path adjusted so business_bookmark_sorter.docx_export imports (it converts and opens in one call), else Pandoc or an equivalent subprocess plus os.startfile on Windows; after that single convert/open succeeds, do not regenerate again—if you must note conversion success in the execution summary, do so only in chat (or leave the Docx status placeholder as written before the one-shot convert); do not leave conversion or open as a manual step; if conversion or open fails, record the exact error and command in the execution summary but still deliver the .md.
