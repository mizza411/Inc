# prompts/

Canonical Cursor agent prompts for Inc Hub workflows. **`agent_formulation_run.txt`** is loaded by the Phase 5 **Agent formulation run** action (edit there; do not hardcode in launcher code).

Agent run outputs live in **`agent-business-idea-runs/outputs/`** (see `agent-business-idea-runs/README.md`).
