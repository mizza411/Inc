Pass 2 — Pack / normalize only (task.md §14). You are NOT inventing a new formulation run: do not add strategies, do not re-rank, do not invent new business ideas, and do not invent fake competitor companies or fake pricing. Open the latest draft under agent-business-idea-runs/outputs/ (usually business_ideas_YYYYMMDD.md for today) produced by Pass 1 Discover (prompts/agent_formulation_run.txt); if the user names a path, use that file. Rewrite the Idea details section so EVERY ranked idea uses the SAME subheads in this order with these exact labels: **Problem:** (or **Problem / trend:**; for S1-traced ideas use **Formula:** as the problem spine plus success signal/citation and **Complaint citations:** with URLs); **Solution:**; **Target:**; **MVP cost:**; **Regulatory:** (required on every card—not only a viability table); **Competitors / alternatives:** (2–4 named products, agencies, or DIY/manual/spreadsheet patterns; each item must include one short “why a Nigeria buyer might still choose this idea” line—Naira/Paystack, niche, language, price, or distribution); **GUEMF:** (Mode A rationale for primary-S12 ideas, or Mode B scores G/U/E/M/F each 1–5 plus composite max 25); **Commercial viability:**; **Dedup:**; **Founder fit:**. Keep strategy add-ons without dropping the shared list: S1 Formula/citations; S12 Mode A GUEMF + evidence URLs; strategy trace in the heading or a bullet. Preserve Best ideas, ranked table, commercial viability snapshot, explicitly-not-re-proposed, execution summary, and next-validation steps—tighten them only if needed so Best ideas still name the top 3–5 and each Best idea’s detail section is fully packed. Incomplete Pass 2 = any ranked idea missing **Regulatory**, **Competitors / alternatives**, **Solution**, **Target**, or **MVP cost**—fill gaps from Pass 1 evidence already in the file; if truly unknown, say so and use an obvious substitute pattern (DIY, accountant, WhatsApp, foreign SaaS). Do not change GUEMF Mode A/B semantics from Pass 1. Contract reference (read-only): prompts/FORMULATION_PASS_CONTRACT.md. Optional before Docx: from repo root run `python agent-business-idea-runs/idea_card_schema.py` on the packed .md (exit 0 required labels OK; soft aid; do not skip packing if it fails—fix cards first). Finish the packed markdown completely first (leave a placeholder only for the Docx conversion status line if needed); then convert/open the Word file exactly once as the final automated step—prefer business_bookmark_sorter.docx_export.regenerate_and_open_docx (sys.path so the package imports; close open Word lock on the same path first if needed)—never convert mid-draft, after small .md tweaks, or a second time to refresh an already-opened document; after that single convert/open succeeds, do not regenerate again—note success in chat only or leave the Docx status placeholder as written before the one-shot convert; if conversion fails, record the exact error in the execution summary but still deliver the packed .md. On Windows use PYTHONIOENCODING=utf-8 for any subprocess helpers.
