# Strategy 8 — dependencies (archived legacy script only)
# Active entrypoint trend_adapter.py is a stub; no packages required.
# Install only if inspecting _archive/trend_adapter_legacy.py:
requests>=2.31.0
beautifulsoup4>=4.12.0
