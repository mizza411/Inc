# Custom tray icon (optional)

Place **`icon.png`** here (64×64 recommended, square).  
If present, the tray app uses it instead of the built-in blue “I” icon.

You can also set `"icon_path"` in `launcher_config.json` → `settings` to any image under `C:\dev\Inc`.
